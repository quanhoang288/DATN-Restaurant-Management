const Joi = require('joi');

const createOrder = {
  body: {},
};

const getOrders = {};

const getOrder = {};

const updateOrder = {};

const deleteOrder = {};

module.exports = {
  createOrder,
  getOrder,
  getOrders,
  updateOrder,
  deleteOrder,
};
